---
# tasks file for license
- name: Render printnanny-license.service from template
  become: true
  ansible.builtin.template:
    mode: 0664
    src: license.service.j2
    dest: /etc/systemd/system/license.service

- name: Create license dir
  ansible.builtin.file:
    path: '{{ printnanny_data_dir }}'
    state: directory
    mode: 0664

- name: Enable printnany-license.service unit
  become: true
  ansible.builtin.systemd:
    name: printnanny-license
    enabled: true
