---
- name: restart janus
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
    name: janus
    enabled: true
    no_block: true

- name: ldconfig
  become: true
  ansible.builtin.command:
    cmd: ldconfig
